<!DOCTYPE html>
<html lang="ja">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Amsac| 自動メール要約・分類システム</title>
    <link rel="stylesheet" href="/stylesheets/add_style.css" />
</head>

<body>
    <header>
        <div class="btn"><span></span><span></span><span></span></div>
        <nav id="menu">
            <form action="" method="post">
                <div>
                    <input type="text" name="tag-add" placeholder="タグを入力" class="textbox" id="tag-add">
                    <input type="submit" value="追加" class="btn-submit" name="htag">
                    <button type="button" id="ai-submit" class="btn-submit-ai" name="atag">自動タグ付け</button>
                </div>
            </form>
            <ul>
                
                <form action="/" >
                    <li name="hp"><a href="/">HOME</a></li>
                </form>

                <form action="/" >
                    <li name="show_all"><a href="/add">Show All Mails</a></li>
                </form>

                <li class="tag-btn">
                    <div class="tag-label">
                        <a href="#">TAG</a>
                        <div class="open-close"><span></span><span></span></div>
                    </div>
                    <ul class="tag-list">
                        <% if (tags && tags.length > 0) { %>
                            <% tags.forEach(function(tag) { %>
                                <li>
                                    <a href="/add?tag=<%= encodeURIComponent(tag.name) %>"><%= tag.name %></a>
                                </li>
                            <% }); %>
                        <% } else { %>
                            <li><p>表示するtagがありません。</p></li>
                        <% } %>
                    </ul>
                </li>

                <li class="tag-btn-ai">
                    <div class="tag-label-ai">
                        <a href="#">AI-TAG</a>
                        <div class="open-close-ai"><span></span><span></span></div>
                    </div>
                    <ul class="tag-list-ai">
                        <% if (ai_tags && ai_tags.length > 0) { %>
                            <% ai_tags.forEach(function(ai_tag) { %>
                                <li>
                                    <a href="/add?aitag=<%= encodeURIComponent(ai_tag.name) %>"><%= ai_tag.name %></a>
                                </li>
                            <% }); %>
                        <% } else { %>
                            <li><p>表示するAI tagがありません。</p></li>
                        <% } %>
                    </ul>
                </li>
                <form action="/" >
                    <li name="favorite"><a href="/add?favorite=true">FAVORITE</a></li>
                </form>
                <form action="/logout" >
                <li name="logout"><a href="/logout">LOGOUT</a></li>
                </form>
                <form action="/" >
                    <li name="delete"><a href="/">Account delete</a></li>
                </form>
            </ul>
        </nav>
    </header>

    <main>
        <section class="centered">
            <h2>メール一覧</h2>

            <% if (emails && emails.length > 0) { %>
                <% emails.forEach(function(email) { %>
                    <p class="todo-description">
                        <%= email.subject %>
                        <button class="fav-btn <%= email.is_favorite ? 'active' : '' %>" name="like" data-id="<%= email.id %>">★</button>
                    </p>
                    <!-- 要約文と原文リンク -->
                    <div class="summary">
                        <p><%= email.summary %></p>
                        <form action="/mail/<%= email.id %>" method="get">
                            <button type="submit">原文を表示</button>
                        </form>
                    </div>
                <% }); %>
            <% } else { %>
                <p>表示するメールがありません。</p>
            <% } %>

            <div class="page-btn">
                <button id="back-btn" class="nextback-btn" name="next">back</button>
                <button id="next-btn" class="nextback-btn" name="back">next</button>
            </div>
        </section>
    </main>
    <script src="/javascripts/favoritebutton.js"></script>
    <script src="/javascripts/add_pagemove.js"></script>

</body>

</html>