<!DOCTYPE html>
<html lang="ja">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Amsac| 自動メール要約・分類システム</title>
    <link rel="stylesheet" href="/stylesheets/add_style.css" />
</head>

<body>
    <header>
        <div class="btn"><span></span><span></span><span></span></div>
        <nav id="menu">
            <form action="" method="post">
                <div>
                    <input type="text" name="tag-add" placeholder="タグを入力" class="textbox" id="tag-add">
                    <input type="submit" value="追加" class="btn-submit" name="htag">
                    <button type="button" id="ai-submit" class="btn-submit-ai" name="atag">自動タグ付け</button>
                </div>
            </form>
            <ul>
                
                <form action="/" >
                    <li name="hp"><a href="/">HOME</a></li>
                </form>
                <li class="tag-btn">
                    <div class="tag-label">
                        <a href="#">TAG</a>
                        <div class="open-close"><span></span><span></span></div>
                    </div>
                    <% if (tags && tags.length > 0) { %>
                        <% tags.forEach(function(tag) { %>
                        <p class="tags">
                            <%= tag.name %>
                        </p>
                    <% }); %>
                    <% } else { %>
                        <p>表示するtagがありません。</p>
                    <% } %>
                </li>
                <li class="tag-btn-ai">
                    <div class="tag-label-ai">
                        <a href="#">AI-TAG</a>
                        <div class="open-close-ai"><span></span><span></span></div>
                    </div>
                    <% if (ai_tags && ai_tags.length > 0) { %>
                        <% ai_tags.forEach(function(ai_tag) { %>
                        <p class="ai_tags">
                            <%= ai_tag.name %>
                        </p>
                    <% }); %>
                    <% } else { %>
                        <p>表示するAI tagがありません。</p>
                    <% } %>
                </li>
                <form action="" >
                <li name="favorite"><a href="/"></a>FAVORITE</li>
                </form>
                <form action="/login" >
                <li name="logout"><a href="/login">LOGOUT</a></li>
                </form>
                <form action="/" >
                    <li name="delete"><a href="/">Account delete</a></li>
                </form>
            </ul>
        </nav>
    </header>

    <main>
        <section class="centered">
            <h2>メール一覧</h2>

            <% if (emails && emails.length > 0) { %>
                <% emails.forEach(function(email) { %>
                <p class="todo-description">
                    <%= email.subject %>
                    <button class="fav-btn" name="like">★</button>
                </p>
                <ul class="todo-list">
                    <li>
                    <%= email.body.replace(/\n/g, "<br>") %>
                    </li>
                </ul>
                <% }); %>
            <% } else { %>
                <p>表示するメールがありません。</p>
            <% } %>
            <div class="page-btn">
                <button id="back-btn" class="nextback-btn" name="next">back</button>
                <button id="next-btn" class="nextback-btn" name="back">next</button>
            </div>
        </section>
    </main>

    <!-- メール要約機能完成後こちらで実装 -->
    <!-- <main>
    <section class="centered">
        <h2>今日のやることリスト</h2>

        <% emails.forEach(function(email, index) { %>
        <p class="todo-description">
            <%= email.subject %>
            <button class="fav-btn" name="like">★</button>
        </p>
        <div class="summary">
            <p><%= email.summary %></p>
            <button class="toggle-original">原文を見る</button>
        </div>
        <div class="original" style="display: none;">
            <p><%= email.original %></p>
            <button class="toggle-summary">要約に戻す</button>
        </div>
        <% }) %>

        <div class="page-btn">
        <button id="back-btn" class="nextback-btn" name="next">back</button>
        <button id="next-btn" class="nextback-btn" name="back">next</button>
        </div>
    </section>
    </main> -->


    <script src="/javascripts/add_pagemove.js"></script>
</body>

</html>