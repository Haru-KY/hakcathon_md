<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>ユーザー登録</title>
  <link rel="stylesheet" href="/stylesheets/register_style.css">
</head>
<body>

  <div class="register-container">
    <h2>ユーザー登録</h2>

    <% if (error) { %>
      <div class="error"><%= error %></div>
    <% } %>

    <form action="/register" method="POST">
      <label for="name">名前</label>
      <input type="text" id="user" name="user" required>

      <label for="password">パスワード</label>
      <input type="password" id="password" name="password" required>

      <label for="repassword">パスワード 再確認</label>
      <input type="password" id="repassword" name="repassword" required>

      <button type="submit">登録</button>
    </form>

    <!-- ページ移動用のリンク -->
    <div class="navigation-links">
      <form action="/login" method="GET">
        <button type="submit" name="login">ログインページへ</button>
      </form>

      <form action="/" method="GET">
        <input type="hidden" name="skipIntro" value="true" />
        <button type="submit" name="hp">ホームに戻る</button>
      </form>

    </div>
  </div>

</body>
</html>
